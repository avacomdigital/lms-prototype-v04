# Variables de entorno
.env
.env.*
*.env
*.env.*
!.env.example
!.env.sample
!.env.template
!*.env.example
!*.env.sample
!*.env.template

# Secretos, credenciales y configuración local
secrets.json
secrets.*.json
*.secrets.json
credentials.json
credentials.*.json
*.credentials.json
local.settings.json
appsettings.Local.json
appsettings.*.Local.json
google-services.json
GoogleService-Info.plist

# Claves, certificados privados y firma de aplicaciones
*.key
*.pem
*.pfx
*.p12
*.ppk
*.snk
*.jks
*.keystore
*.mobileprovision

# Compilaciones de .NET y .NET MAUI
**/bin/
**/obj/
**/artifacts/
**/publish/
TestResults/
*.trx
*.coverage
*.coveragexml

# Paquetes generados por las plataformas MAUI
*.apk
*.aab
*.ipa
*.appx
*.appxbundle
*.msix
*.msixbundle
*.xcarchive/
*.dSYM/

# NuGet
packages/
*.nupkg
*.snupkg

# Archivos locales de IDE y usuario
.vs/
.idea/
*.suo
*.user
*.userosscache
*.sln.docstates

# Archivos temporales del sistema operativo
.DS_Store
Thumbs.db
Desktop.ini
*.tmp
*.temp
*.log


# Backend Django (AVACOM LMS)
backend/.venv/
backend/db.sqlite3
backend/**/__pycache__/

# Instalador (installer/build/Build-Installer.ps1)
# Contenido intermedio: la "version instalable" antes de empaquetarla.
dist/
# El .exe ronda los 94 MB, muy cerca del limite de 100 MB por archivo que
# acepta un push a GitHub, y cada version añadiria otro tanto al historial.
# Se publica como release asset; en el repositorio quedan el codigo que lo
# reconstruye y SHA256.txt para verificar la descarga.
installer/latest/*.exe
# Arnes de prueba del asistente: se compila y se borra en cada verificacion.
installer/build/PruebaAsistente.exe
